version: "2"
sql:
  - schema: "schema.sql"
    queries: 
      - "db/queries.sql"
      - "db/queries-templates.sql"
    engine: "postgresql"
    gen:
      go:
        package: "db"
        out: "db"
        sql_package: "pgx/v5"
        emit_json_tags: true
